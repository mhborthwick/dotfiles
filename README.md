# dotfiles

> **Warning:** This repository is primarily for personal use. Use at your own risk.

## Dependencies

- **shfmt** — shell script formatter

## Symlinks

`make sync` — symlinks repo files into `$HOME`.

## Format

`make format` — runs shfmt on shell and config files.
